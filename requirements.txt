# ============================================================================
# SDR FSK 项目依赖库
# ============================================================================
# 安装方法: pip3 install -r requirements.txt
# 注意: SoapySDR 建议通过系统包管理器安装 (sudo apt install python3-soapysdr)
# ============================================================================

# 数值计算
numpy>=1.21.0

# 信号处理 (滤波器设计、频谱分析)
scipy>=1.7.0

# 可视化 (频谱图、调试图表)
matplotlib>=3.4.0

# SDR 驱动 (可选，推荐通过 apt 安装)
# SoapySDR  # 注释掉，因为通过系统包管理器安装更可靠
